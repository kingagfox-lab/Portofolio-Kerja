Icons are rendered with Lucide Icons via CDN (see the <script> tag for
lucide in each page). No local SVG icon files are needed. If you want to
vendor icons locally, drop individual SVGs here and swap the <i data-lucide="...">
tags for inline <img>/<svg> references.
